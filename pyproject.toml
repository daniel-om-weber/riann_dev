[project]
name = "riann"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "tsfast",
    "matplotlib",
]

[project.optional-dependencies]
prep = ["scipy", "h5py", "pandas", "requests", "tqdm"]

[tool.uv.sources]
tsfast = { path = "../tsfast", editable = true }
